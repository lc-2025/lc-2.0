import '../env';

// Utilities - Environment Variables
const { NEXT_PUBLIC_PROJECT_ID, NEXT_PUBLIC_DATASET, NEXT_PUBLIC_API_VERSION } =
  process.env;

export { NEXT_PUBLIC_PROJECT_ID, NEXT_PUBLIC_DATASET, NEXT_PUBLIC_API_VERSION };
